#include <iostream>
#include <cmath>

struct point {
/*asdjfjasdf
  */
  int x, y;
  explicit point(int a = 0, int b = 0) {
    x = a;
    y = b;
  }
  long double len() {
    return std::sqrt(x*x + y*y);
  }
  point ort() {
    return point(-y, x);
  }
  point norm() {
    // return this.len();
  }
  long double operator-() {

  }
  long double f1(const point &a, const point &b) {
    return a.x * b.x + a.y * b.y;
  }
};

int main() {

  return 0;
}
